{
  "baseBranch": "origin/main",
  "pipeline": {
    "lint": {},
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "dist/**",
        ".next/**",
        "build/**"
      ]
    }
  }
}